name: CI sandbox

on: [push, pull_request, workflow_dispatch]

jobs:
  test:
    runs-on: 'windows-latest'
    name: Test
    steps:
      - uses: actions/checkout@v6
      - name: test1
        run: python3 inspect.py
